***输入cmd，进入命令提示窗口

输入netstat -ano 显示端口占用情况

输入netstat -ano|findstr "8080" 显示指定的端口占用情况

输入tasklist|findstr "5264" 查找指定PID 对应的进程，看看哪个进程占用了端口

***任务栏管理器查看PID对应的进程

菜单栏————查看---选择列---PID（进程表示服）  打钩，，点击确定，然后在进程列表卡 就能 看到进程对应的PID了
